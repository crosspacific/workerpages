name: Auto Update IPTV (Smart Schedule)

on:
  schedule:
    # 每4小时运行一次 (UTC时间: 0, 4, 8, 12)
    # 对应北京时间: 08, 12, 16, 20 点
    - cron: '0 0,4,8,12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  smart-random-update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install cloudscraper requests

      - name: Smart Update Check
        env:
          IPTV_URL: ${{ secrets.IPTV_URL }}
        run: |
          python -c "
          import cloudscraper
          import os
          import sys
          import time
          import random
          import subprocess
          from datetime import datetime, timezone, timedelta

          # === 核心配置修改 ===
          FILE_NAME = 'list.m3u'
          MIN_INTERVAL = 14400   # 4小时 (4 * 3600)
          MAX_INTERVAL = 28800   # 8小时 (8 * 3600)
          
          def get_last_commit_time(filename):
              if not os.path.exists(filename):
                  print(f'File {filename} not found locally. Assuming first run.')
                  return 0
              try:
                  result = subprocess.check_output(['git', 'log', '-1', '--format=%ct', filename])
                  return int(result.strip())
              except Exception as e:
                  print(f'Git log failed: {e}')
                  return 0

          # 打印北京时间
          tz_bj = timezone(timedelta(hours=8))
          now_bj = datetime.now(tz_bj)
          now_str = now_bj.strftime('%Y-%m-%d %H:%M:%S')
          print(f'Current Beijing Time: {now_str}')

          last_time = get_last_commit_time(FILE_NAME)
          current_time = int(time.time())
          time_diff = current_time - last_time
          hours_ago = time_diff / 3600
          
          print(f'Last update was {hours_ago:.2f} hours ago.')

          should_run = False
          
          if last_time == 0:
              print('>>> First run or file missing. Forcing update.')
              should_run = True
          elif time_diff < MIN_INTERVAL:
              # 小于 4 小时，坚决不更新
              print(f'>>> Too soon (< 4h). Skipping.')
              sys.exit(0)
          elif time_diff > MAX_INTERVAL:
              # 超过 8 小时，强制更新
              print(f'>>> Over 8 hours ({hours_ago:.2f}h). Forcing update.')
              should_run = True
          else:
              # 4-8 小时之间，50% 概率触发更新
              if random.choice([True, False]):
                  print('>>> Random check (4h-8h): SUCCESS. Updating now.')
                  should_run = True
              else:
                  print('>>> Random check (4h-8h): SKIP. Waiting for next cycle.')
                  sys.exit(0)

          if should_run:
              url = os.environ.get('IPTV_URL')
              if not url:
                  print('Error: Secret IPTV_URL is not set')
                  sys.exit(1)

              try:
                  # 随机延迟 5-180 秒，模拟人类行为
                  sleep_time = random.randint(5, 180)
                  print(f'Sleeping for {sleep_time} seconds...')
                  time.sleep(sleep_time)

                  scraper = cloudscraper.create_scraper(browser='chrome')
                  response = scraper.get(url)
                  
                  if response.status_code == 200:
                      if '#EXTM3U' in response.text:
                          with open(FILE_NAME, 'w', encoding='utf-8') as f:
                              f.write(response.text)
                          print('Success: File downloaded.')
                      else:
                          print('Warning: Content does not look like M3U.')
                  else:
                      print(f'Error: Status code {response.status_code}')
                      sys.exit(1)
              except Exception as e:
                  print(f'Error: {str(e)}')
                  sys.exit(1)
          "

      - name: Commit and Push
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          
          if [ -f list.m3u ]; then
            git add list.m3u
            if git diff --staged --quiet; then
              echo "Content unchanged. Skipping commit."
              exit 0
            fi
            git commit -m "Update: $(date +'%Y-%m-%d %H:%M:%S')"
            git push
          fi
